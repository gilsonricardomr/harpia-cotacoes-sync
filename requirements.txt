# Dependências do Sistema de Sincronização de Cotações

# HTTP
requests>=2.31.0
beautifulsoup4>=4.12.0

# curl_cffi — emula fingerprint TLS do Chrome, bypassa Cloudflare
# Usado pelo Investing.com (funciona em datacenter/VPS/GitHub Actions)
curl_cffi>=0.7.0

# Selenium — usado pelo CEPEA (feijão, arroz)
selenium>=4.15.0
webdriver-manager>=4.0.0

# Supabase
supabase>=2.0.0
httpx>=0.25.0
postgrest>=0.10.0
gotrue>=1.0.0
